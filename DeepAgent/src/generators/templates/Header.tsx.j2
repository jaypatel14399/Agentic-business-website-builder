'use client'

import Link from 'next/link'
import { useState } from 'react'
import DarkModeToggle from './DarkModeToggle'

export default function Header() {
  const [isMenuOpen, setIsMenuOpen] = useState(false)

  return (
    <header className="bg-[var(--theme-background,var(--color-background))]/95 backdrop-blur-sm shadow-md sticky top-0 z-50 border-b border-[var(--theme-border,var(--color-border,rgba(0,0,0,0.1)))]">
      <nav className="container mx-auto max-w-6xl px-4">
        <div className="flex items-center justify-between h-16">
          {/* Logo/Brand */}
          <Link href="/" className="font-heading text-2xl font-bold text-[var(--theme-text,var(--color-primary))] hover:opacity-80 transition-colors">
            {{ business.name }}
          </Link>

          {/* Desktop Navigation */}
          <div className="hidden md:flex items-center space-x-6">
            <DarkModeToggle />
            <Link
              href="/"
              className="text-[var(--theme-text,var(--color-text))] hover:opacity-80 font-medium transition-colors"
            >
              Home
            </Link>
            <Link
              href="/about"
              className="text-[var(--theme-text,var(--color-text))] hover:opacity-80 font-medium transition-colors"
            >
              About
            </Link>
            <Link
              href="/#services"
              className="text-[var(--theme-text,var(--color-text))] hover:opacity-80 font-medium transition-colors"
            >
              Services
            </Link>
            <Link
              href="/contact"
              className="bg-[var(--theme-primary,var(--color-primary))] text-[var(--theme-onPrimary,#fff)] px-6 py-2.5 rounded-xl font-medium hover:opacity-90 transition-all duration-300"
            >
              Contact
            </Link>
          </div>

          {/* Mobile: Dark mode + Menu Button */}
          <div className="flex md:hidden items-center gap-2">
            <DarkModeToggle />
          </div>
          <button
            className="md:hidden text-[var(--theme-text,var(--color-text))] hover:opacity-80 transition-colors"
            onClick={() => setIsMenuOpen(!isMenuOpen)}
            aria-label="Toggle menu"
          >
            <svg
              className="w-6 h-6"
              fill="none"
              strokeLinecap="round"
              strokeLinejoin="round"
              strokeWidth="2"
              viewBox="0 0 24 24"
              stroke="currentColor"
            >
              {isMenuOpen ? (
                <path d="M6 18L18 6M6 6l12 12" />
              ) : (
                <path d="M4 6h16M4 12h16M4 18h16" />
              )}
            </svg>
          </button>
        </div>

        {/* Mobile Navigation */}
        {isMenuOpen && (
          <div className="md:hidden py-4 space-y-4">
            <Link
              href="/"
              className="block text-[var(--theme-text,var(--color-text))] hover:opacity-80 font-medium"
              onClick={() => setIsMenuOpen(false)}
            >
              Home
            </Link>
            <Link
              href="/about"
              className="block text-[var(--theme-text,var(--color-text))] hover:opacity-80 font-medium"
              onClick={() => setIsMenuOpen(false)}
            >
              About
            </Link>
            <Link
              href="/#services"
              className="block text-[var(--theme-text,var(--color-text))] hover:opacity-80 font-medium"
              onClick={() => setIsMenuOpen(false)}
            >
              Services
            </Link>
            <Link
              href="/contact"
              className="block bg-[var(--theme-primary,var(--color-primary))] text-[var(--theme-onPrimary,#fff)] px-6 py-2.5 rounded-xl font-medium text-center"
              onClick={() => setIsMenuOpen(false)}
            >
              Contact
            </Link>
          </div>
        )}
      </nav>
    </header>
  )
}

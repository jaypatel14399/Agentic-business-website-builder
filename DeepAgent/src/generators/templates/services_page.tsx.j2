import ServiceCard from '@/components/ServiceCard'
import SEO from '@/components/SEO'

export const metadata = {
  title: 'Our Services | {{ business.name }}',
  description: 'Professional {{ business.industry }} services in {{ business.city }}{% if business.state %}, {{ business.state }}{% endif %}',
}

export default function ServicesPage() {
  const services = {{ services | tojson }};

  return (
    <>
      <SEO />
      
      {/* Hero Section */}
      <section className="bg-gradient-to-r from-primary-600 to-primary-800 text-white py-16 px-4">
        <div className="container mx-auto max-w-4xl text-center">
          <h1 className="text-5xl font-bold mb-6">
            Our Services
          </h1>
          <p className="text-xl text-primary-100">
            Professional {{ business.industry }} services in {{ business.city }}{% if business.state %}, {{ business.state }}{% endif %}
          </p>
        </div>
      </section>

      {/* Services Grid */}
      <section className="py-16 px-4">
        <div className="container mx-auto max-w-6xl">
          {% if services and services|length > 0 %}
          <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            {services.map((service: any, index: number) => (
              <ServiceCard key={index} service={service} />
            ))}
          </div>
          {% else %}
          <div className="text-center py-16">
            <p className="text-xl text-gray-600">
              Services information coming soon!
            </p>
          </div>
          {% endif %}
        </div>
      </section>

      {/* Why Choose Us Section */}
      <section className="py-16 px-4 bg-gray-50">
        <div className="container mx-auto max-w-4xl">
          <div className="text-center mb-12">
            <h2 className="text-4xl font-bold text-gray-900 mb-4">
              Why Choose {{ business.name }}?
            </h2>
          </div>
          
          <div className="grid grid-cols-1 md:grid-cols-2 gap-8">
            <div className="bg-white p-6 rounded-lg shadow-md">
              <h3 className="text-xl font-semibold text-primary-600 mb-3">
                Experienced Professionals
              </h3>
              <p className="text-gray-700">
                Our team brings years of experience in {{ business.industry }} services to every project.
              </p>
            </div>
            
            <div className="bg-white p-6 rounded-lg shadow-md">
              <h3 className="text-xl font-semibold text-primary-600 mb-3">
                Quality Service
              </h3>
              <p className="text-gray-700">
                We're committed to delivering the highest quality workmanship and customer service.
              </p>
            </div>
            
            <div className="bg-white p-6 rounded-lg shadow-md">
              <h3 className="text-xl font-semibold text-primary-600 mb-3">
                Local Expertise
              </h3>
              <p className="text-gray-700">
                Serving {{ business.city }}{% if business.state %}, {{ business.state }}{% endif %} and surrounding areas with local knowledge and expertise.
              </p>
            </div>
            
            <div className="bg-white p-6 rounded-lg shadow-md">
              <h3 className="text-xl font-semibold text-primary-600 mb-3">
                Customer Satisfaction
              </h3>
              <p className="text-gray-700">
                Your satisfaction is our priority. We work closely with you to ensure your needs are met.
              </p>
            </div>
          </div>
        </div>
      </section>

      {/* Contact CTA */}
      <section className="py-16 px-4 bg-primary-600 text-white">
        <div className="container mx-auto max-w-4xl text-center">
          <h2 className="text-3xl font-bold mb-4">
            Ready to Get Started?
          </h2>
          <p className="text-xl mb-8 text-primary-100">
            Contact us today to discuss your {{ business.industry }} needs.
          </p>
          <a
            href="/contact"
            className="inline-block bg-white text-primary-600 px-8 py-3 rounded-lg font-semibold hover:bg-primary-50 transition-colors shadow-lg"
          >
            Get Your Free Quote
          </a>
        </div>
      </section>
    </>
  )
}

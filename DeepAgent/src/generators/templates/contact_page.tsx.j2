import ContactForm from '@/components/ContactForm'
import SEO from '@/components/SEO'
import Footer from '@/components/Footer'

export const metadata = {
  title: {{ ('Contact Us | ' ~ business.name) | tojson }},
  description: {{ ('Get in touch with ' ~ business.name ~ '. Located in ' ~ business.city ~ (', ' ~ business.state if business.state else '') ~ '.') | tojson }},
}

export default function ContactPage() {
  return (
    <>
      <SEO />
      
      {/* Hero Section */}
      <section className="bg-primary text-white py-20 px-4">
        <div className="container mx-auto max-w-4xl text-center">
          <h1 className="font-heading text-5xl md:text-6xl font-bold mb-6">
            {{ contact.form_title }}
          </h1>
          {% if contact.form_description %}
          <p className="text-xl text-white/90">
            {{ contact.form_description }}
          </p>
          {% endif %}
        </div>
      </section>

      {/* Contact Section */}
      <section className="py-20 px-4">
        <div className="container mx-auto max-w-6xl">
          <div className="grid grid-cols-1 lg:grid-cols-2 gap-12">
            {/* Contact Form */}
            <div>
              <h2 className="font-heading text-3xl font-bold text-content mb-6">
                Send Us a Message
              </h2>
              <ContactForm />
            </div>

            {/* Contact Information */}
            <div>
              <h2 className="font-heading text-3xl font-bold text-content mb-6">
                Contact Information
              </h2>
              
              <div className="space-y-6">
                <div className="bg-surface p-6 rounded-xl border border-primary/10">
                  <h3 className="font-heading text-xl font-semibold text-primary mb-2">
                    Business Name
                  </h3>
                  <p className="text-gray-700">
                    {{ business.name }}
                  </p>
                </div>

                {% if contact.address_display %}
                <div className="bg-gray-50 p-6 rounded-lg">
                  <h3 className="text-xl font-semibold text-primary-600 mb-2">
                    Address
                  </h3>
                  <p className="text-gray-700 whitespace-pre-line">
                    {{ contact.address_display }}
                  </p>
                </div>
                {% endif %}

                {% if contact.phone_display and contact.phone_display != "N/A" %}
                <div className="bg-gray-50 p-6 rounded-lg">
                  <h3 className="text-xl font-semibold text-primary-600 mb-2">
                    Phone
                  </h3>
                  <p className="text-gray-700">
                    <a href="tel:{{ business.phone }}" className="hover:text-primary-600">
                      {{ contact.phone_display }}
                    </a>
                  </p>
                </div>
                {% endif %}

                <div className="bg-primary-50 p-6 rounded-lg">
                  <h3 className="text-xl font-semibold text-primary-600 mb-2">
                    Service Area
                  </h3>
                  <p className="text-gray-700">
                    {{ business.city }}{% if business.state %}, {{ business.state }}{% endif %} and surrounding areas
                  </p>
                </div>
              </div>

              {% if business.rating %}
              <div className="mt-8 bg-white p-6 rounded-lg shadow-md border-2 border-primary-200">
                <div className="flex items-center mb-2">
                  <span className="text-2xl font-bold text-gray-900 mr-2">
                    {{ business.rating }}
                  </span>
                  <div className="flex text-yellow-400">
                    {% for i in range(5) %}
                    <svg className="w-5 h-5 {% if i < (business.rating|int) %}text-yellow-400{% else %}text-gray-300{% endif %}" fill="currentColor" viewBox="0 0 20 20">
                      <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z" />
                    </svg>
                    {% endfor %}
                  </div>
                </div>
                <p className="text-gray-600">
                  Rated business on Google
                </p>
              </div>
              {% endif %}
            </div>
          </div>
        </div>
      </section>

      {/* Map Section (Optional - placeholder) */}
      <section className="py-16 px-4 bg-gray-50">
        <div className="container mx-auto max-w-6xl">
          <h2 className="text-3xl font-bold text-gray-900 mb-6 text-center">
            Find Us
          </h2>
          <div className="bg-white p-8 rounded-lg shadow-md text-center">
            <p className="text-gray-700 mb-4">
              {{ contact.address_display }}
            </p>
            <p className="text-sm text-gray-500">
              Map integration can be added here (Google Maps, Mapbox, etc.)
            </p>
          </div>
        </div>
      </section>
      <Footer />
    </>
  )
}
